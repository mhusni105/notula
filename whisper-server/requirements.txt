# OpenAI Whisper Local Server
# Middleware untuk Notula — menyediakan STT lokal via HTTP API.
# Model tetap dimuat di memori agar transkripsi cepat.

fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
openai-whisper>=20231117
